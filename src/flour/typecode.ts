export enum FlourBoxedTypeCode {
    PAIR,
    SYMBOL,
    CLOSURE,
    // STRING,
    // VECTOR
};

export enum FlourUnboxedTypeCode {
    NIL = 0,
    FIXNUM,
    BOOLEAN,
    CHARACTER,
    FUNC_PTR,
    PTR
};